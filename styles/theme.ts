import { extendTheme } from '@chakra-ui/react';

export const careermapTheme = extendTheme({
  colors: {
    brand: {
      bg: '#06020d',
      hero: '#06020d',
      footer: '#0d041e'
    }
  }
});
